{# {{ attach_library('test_Test/left-menu-js') }} #}

{% if headerauto == 1 %}
    {% if unavsetting == '1' %}
        <nav id="test-header" class="test-header {{ headerformat }} alt-unav" aria-label="primary-site" >
    {% else %}
        <nav id="test-header" class="test-header {{ headerformat }}" aria-label="primary-site" >
    {% endif %}
        <div class="nav-toggle">
            <a href="#" role="button" id="test-menu-control">Navigation menu</a>
        </div>
    {% if agencylogo is not null and agencylogo is not empty %}
        <div class="test-logo"><a href="/"><img src="{{ agencylogo }}" alt="{{ agencyname|raw }}" /></a></div>
    {% else %}
        {# <h1><a href="/">{{ agencyname|raw }}</a></h1> #}
    {% endif %}
        {{ renderMenu(headermenu, 'header', 'none') }}
    </nav>
{% endif %}